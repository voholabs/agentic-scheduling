{
  "name": "postiz-orchestrator",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "dotenv -e ../../.env -- nest start --watch --entryFile=./apps/orchestrator/src/main",
    "build": "cross-env NODE_ENV=production nest build",
    "start": "dotenv -e ../../.env -- node --experimental-require-module ./dist/apps/orchestrator/src/main.js",
    "pm2": "cross-env DOTENV_CONFIG_PATH=../../.env pm2 start node --name orchestrator --restart-delay 3000 -- --experimental-require-module -r dotenv/config ./dist/apps/orchestrator/src/main.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
